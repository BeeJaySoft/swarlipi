# What to type

Every symbol and the character that produces it. This table and the notation
app's in-editor guide render from the same data, `swarlipi/reference`, so the
two cannot drift. [Symbols](/notation/) shows the same marks drawn large, with
the geometry explained.

<SheetControls />

<SymbolTable />
