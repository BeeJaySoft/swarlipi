<!--
Thanks for the pull request. This repository mirrors the swarlipi package
from a private monorepo, so it is not merged here: once reviewed, it is
applied upstream with your authorship kept, lands with the next mirror push,
and is then closed with a pointer to that commit.
-->

**What changes, and why**

**Checks**: `pnpm test`, `pnpm typecheck` and `pnpm docs:build` pass.
